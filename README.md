# Clinical Reader Study

A single-page HTML application for a clinical reader study with **30 cases × 4 sections** (120 total sections).

Built with vanilla HTML5, CSS, and JavaScript — no dependencies.

## Deploy

This repo is ready for GitHub Pages. Push to `main` and enable Pages from the root → `index.html`.
